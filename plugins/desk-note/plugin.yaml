name: desk-note
version: 0.1.0
description: "Injects the escalating desk-state note when the context fills, so the model curates. Stage 4 of the model-driven context-curation feature."
author: "wpu / context-curation"
hooks:
  - pre_llm_call
