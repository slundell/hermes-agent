name: desk-ids
version: 0.1.0
description: "Stamps a consecutive [bN] block id onto every tool result — the addressable handle the model uses to curate its context (archive/recall). Stage 1 of the model-driven context-curation feature."
author: "wpu / context-curation"
hooks:
  - transform_tool_result
